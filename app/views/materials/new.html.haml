=form_for [plant, material] do |f|
	%legend= "New Material for " + plant.name
	-material.class.attr_accessible[:default].each do |attr|
		-next if attr.blank?
		=f.label attr
		=f.text_field attr
	=f.submit